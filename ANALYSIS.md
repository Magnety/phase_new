# 现有 refine 结果与数据诊断

本诊断只把“验证/测试反向传播”视为临时实验设置；PHASE 恢复严格只读评估，
但不把该设置当作模型失效的根因。

## 已有结果

现有最终测试结果：

| Task | n | Accuracy | AUROC | AUPRC | Brier |
|---|---:|---:|---:|---:|---:|
| pCR | 204 | 0.603 | 0.612 | 0.339 | 0.334 |
| HER2 | 164 | 0.622 | 0.627 | 0.433 | 0.303 |

pCR 概率在负类为 `0.356±0.412`、正类为 `0.474±0.417`；HER2 在负类为
`0.321±0.365`、正类为 `0.445±0.392`，均存在大范围重叠。pCR 的中心 AUROC
约从 0.480 到 0.689，已有 leave-one-centre 结果更达到 0.455--0.864，说明中心敏感。

对最终测试集导出的 384 维 pCR 任务特征使用完全相同的标准化、PCA-20、
balanced logistic regression 和 5 折交叉验证：

- pCR 标签探针 AUROC：`0.526±0.084`；
- 中心探针 balanced accuracy：`0.660±0.099`；
- 5 中心 balanced chance：`0.200`。

因此“中心关联强于任务类别”不只是 t-SNE 视觉印象，而是可以复现的线性可分性差异。

## 数据机制

按中心-患者去重后，manifest 中 pCR 阳性率约为 0.205--0.434，HER2 为 0.236--0.497；
部分队列还存在结构性 HER2 缺失。旧配置只要求 pCR 标签才入组，并且中心平衡采样、
跨中心监督对比、中心对齐和域对抗均只针对 pCR。其后果是：

- HER2 的有效样本和中心覆盖被 pCR 入组条件二次限制；
- 中心与标签先验、标签缺失、扫描协议可共同构成捷径；
- pCR 的强逆频重复采样会反复出现小中心/稀有类病例，加重记忆；
- HER2 没有与 pCR 等价的中心不变约束。

## 模型机制

原任务端是 384 维、12 expert、top-4 MoE，并接多层任务头；相对于 pCR/HER2
有效标签规模，其可训练容量足以记忆中心和病例。另一方面，分类隔离路径对 DCE
时相主要做无序影像聚合，动态信息依赖旁路 PINN 补回，容易同时出现欠拟合和过拟合。
PHASE v3 的 MoE 被拆成两个受约束层级：缺失感知的模态 top-k 路由和显式任务条件的
6-expert/top-2 路由；二者都导出逐病例权重并受中心稳健目标约束。PINN 在预训练直接拟合
药代曲线，并把参数和拟合曲线摘要真正送入分类特征。

旧主实验还要求每位患者同时存在 `T0+LAST`。在真实 dataset 构建后，这一条件只保留
1322 名患者并把 Duke 整个中心排除；末次访视是否存在、何时采集和治疗路径也高度依赖
中心。PHASE 主实验因此使用治疗前 `T0`，但不会丢掉 T0 单次检查内部的原始 DCE 多时相
曲线。纵向 `T0+LAST` 仅作为预先声明的独立消融，不用于主模型选择。

## PHASE v5 数据审计

PHASE 自己解析完整 manifest 后得到：

| 阶段 | 病例数 | 中心数 | train/val/test |
|---|---:|---:|---:|
| 无标签预训练 | 3405 | 9 | 约 2383 / 511 / 511 |
| 任一启用任务有标签的微调 | 1850 | 7 | 1280 / 285 / 285 |

微调训练/验证/测试的 pCR 有效标签数分别为 1213/270/269；HER2 为
1073/236/225；ER 与 PR 均为 613/139/139；HR 为 1074/238/225；分子分型为
522/118/125；生存为 175/43/42，其中事件为 9/5/7。测试集由 refine 相同的
`SHA1(seed|dataset_id|patient_id)` 边界先固定，再只在训练+验证池内做中心×pCR 分层。
三个集合按“中心+患者”校验后交集为零。原始清单中 T1/T2/DWI/ADC 分别覆盖
778/2707/515/461 个基线病例，非 DCE 模态均可缺失。原始 DCE 实例抽查能够得到
8 个有效时相，重采样张量全部有限，跨时相标准化后的均值约为 0、标准差为 1。

## 四个问题与代码机制

| 问题 | 主要机制 | 防止副作用的约束 |
|---|---|---|
| pCR/HER2 效果低 | 分层 3-D Transformer 表型、DCE/非 DCE token-MAE、双层时相注意力、药代 PINN、多模态 MoE、任务 MoE 和独立原型头 | 缺失模态权重严格为零；任务容量用局部窗口、浅层辅助骨干、top-k/均衡损失/域稳健约束 |
| 中心差异、泛化差 | 预训练和微调均有梯度反转中心判别器，微调加最差组风险 | 只在同类别内做中心对齐，避免删掉疾病类别信号 |
| 特征更关联中心 | detached 风格分支主动吸收扫描信息，临床表型与风格正交；逐任务导出中心/标签探针 | 中心 ID 不进入任何预测特征或分类头 |
| 欠拟合/过拟合 | 自监督扩大有效样本，动力学减轻欠拟合；低维任务适配器、原型分类、分阶段解冻、强正则和早停抑制过拟合 | 只用验证集全局+最差中心 AUROC 选模型，训练中完全不构造测试 loader |

预训练不是旧 checkpoint 的继续训练，而是 PHASE 全部 DCE/非 DCE 编码器、模态 MoE 和
PINN 从随机参数开始，联合执行双视图 VICReg、token 掩蔽体素重建、extended-Tofts 曲线/ODE
拟合、时相顺序学习、中心对抗、风格中心分类和表型/风格正交。
微调默认拒绝随机初始化：必须使用 PHASE 预训练 checkpoint，除非显式打开只用于消融的
`allow_random_initialization`。

## 指标结论边界

架构设计可以针对问题建立可检验机制，但在完整训练完成前不能诚实地保证某个未知测试集
数值一定提高。因此代码把“提高”实现为不可含糊的验收门：冻结测试 pCR AUROC>0.612、
accuracy>0.603，HER2 AUROC>0.627、accuracy>0.622 四项必须同时成立，`summary.json`
中的 `acceptance.passed` 才为 true。即使四项通过，仍需结合最差中心 AUROC、中心探针、
Brier、多随机种子与 leave-one-centre-out 结果判断是否真正消除了中心捷径，而不是重新挑选
一次幸运划分。
